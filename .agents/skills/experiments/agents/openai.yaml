interface:
  display_name: "Experiments"
  short_description: "Repo-specific patterns and workflows for experiments"
  default_prompt: "Use the experiments repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true